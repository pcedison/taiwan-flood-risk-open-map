# Geo Package

Shared geospatial placeholders for map styles, tile configuration, and spatial helpers.


## Status: placeholder (not yet implemented)

This directory is scaffolding for a planned shared workspace package, not a
working one. The `*.placeholder.yml` files and stub READMEs mark intended
structure only — the live rules/config still live inside `apps/*`. Do not
import from here expecting behavior. Tracked as ARCH F3-A in
`docs/reviews/audit-2026-07-06-architecture.md`: either wire these into the
app/contract tests when the shared layer is built, or retire the directory.
